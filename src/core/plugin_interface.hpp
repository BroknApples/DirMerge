#ifndef PLUGIN_INTERFACE_HPP
#define PLUGIN_INTERFACE_HPP


// Learn how to create plugins and use them to add to the program.
// Plan on plugins being able to modify specific spots of the
// program depending on what virtual funcs are implemented
//
// there should be tabs on the "merge_settings" panel on the bottmo that
// correspond to each plugin, like how by default vscode has "terminal"
// but more can be added to the dock


// each file in general
// each file while doing the merge

// etc.


class PluginInterface {
  // TODO: Implement after setting up the base program
};


#endif // PLUGIN_INTERFACE_HPP